name: Development Snapshot

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-24.04
    container: ghcr.io/codables/swift-android-sdk:latest
    steps:
      - name: Hello
        id: hello
        run: echo Android NDK is ${ANDROID_NDK_ROOT}